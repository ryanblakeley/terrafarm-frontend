{
  "apps": [
    {
      "name": "csa-frontend",
      "script": "./build/dist/server.js"
    }
  ],
  "deploy": {
    "production": {
      "user": "git",
      "host": "138.68.14.104", // replace with public IP for CSA frontend server
      "ref" : "origin/master",
      "repo": "git@github.com:rojobuffalo/csa-frontend.git",
      "path": "/srv/repo/csa-frontend",
      "pre-deploy-local" : "echo 'Preparing to deploy to production...'",
      "post-deploy": "npm install ; npm run build ; pm2 startOrRestart ecosystem.json --env production",
      "env": {
        "NODE_ENV": "production"
      }
    }
  }
}
