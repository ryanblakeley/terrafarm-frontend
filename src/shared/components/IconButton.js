import React from 'react';
import MaterialIconButton from 'material-ui/IconButton';

const IconButton = props => <MaterialIconButton
  {...props}
/>;

IconButton.propTypes = {
};

IconButton.defaultProps = {
};

export default IconButton;
